#define CATCH_CONFIG_MAIN  // This tells Catch to provide a main() - only do this in one cpp file
#include "catch.hpp"

namespace TanksGameUnitTests
{
    TEST_CASE("When Game ctor invoked and graphicsEngine is set to NULL should throw invalid argument exception", "[factorial]") {

        REQUIRE(1 == 1);
    }

    TEST_CASE("Factorials are computeddkjbfsdlkjb", "[factorial]") {
        REQUIRE(1 == 1);
    }
}
